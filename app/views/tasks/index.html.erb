<% @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <p><%= task.description %></p>
  <p><%= task.done %></p>
<% end %>

<% @tasks.each do |task| %>
  <%= link_to task.name, task_path(task) %>
<% end %>

